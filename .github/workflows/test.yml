name: Test

on:
  workflow_dispatch:

jobs:
  build-and-push-escalation:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Build
      run: |
        make build-and-push-escalation
